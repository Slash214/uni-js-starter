# `src/env` 说明

此处放**业务环境配置**（如功能开关、渠道常量），与仓库根目录 **Vite 环境变量**（`.env` / `.env.development`）区分：

| 用途           | 位置              |
| -------------- | ----------------- |
| 构建期注入变量 | 根目录 `.env*`    |
| 业务侧常量/映射 | `src/env/*.js`   |

不要在 `src/env` 存放密钥；密钥走服务端或小程序后台配置。
